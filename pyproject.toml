[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "steamggames"
version = "1.0.0"
description = "SteamGGames — CLI + TUI for browsing games on steamgg.net"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
  { name = "Troli", email = "contato@pablotroli.com" }
]
maintainers = [
  { name = "steamggames contributors" }
]
keywords = [
  "steamgg",
  "games",
  "scraper",
  "cli",
  "tui",
  "terminal",
  "browser",
  "cross-platform",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: End Users/Desktop",
  "License :: OSI Approved :: MIT License",
  "Operating System :: POSIX :: Linux",
  "Operating System :: MacOS :: MacOS X",
  "Operating System :: Microsoft :: Windows",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Games/Entertainment",
  "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
  "Topic :: Utilities",
]
dependencies = [
  "aiohttp>=3.9.0",
  "aiofiles>=23.2.0",
  "beautifulsoup4>=4.12.0",
  "certifi",
  "rich>=13.7.0",
  "urllib3>=2.0.0",
  "Brotli>=1.1.0",
]

[project.urls]
Homepage = "https://github.com/DevTroli/SteamGGames"
Repository = "https://github.com/DevTroli/SteamGGames"
Issues = "https://github.com/DevTroli/SteamGGames/issues"
Changelog = "https://github.com/DevTroli/SteamGGames/blob/main/CHANGELOG.md"

[project.scripts]
steamggames = "steamggames.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.0",
  "ruff>=0.6.0",
  "mypy>=1.10.0",
]

[tool.hatch.build.targets.wheel]
packages = ["steamggames"]
include = ["LICENSE", "README.md"]

[tool.hatch.build.targets.sdist]
include = ["LICENSE", "README.md", "pyproject.toml", "steamggames/"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
